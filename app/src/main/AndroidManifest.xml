<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 应用内自更新：下载新版 APK 后拉起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DshBridge"
        android:usesCleartextTraffic="true">
        <!-- usesCleartextTraffic 必须为 true：dsh-bridge 的局域网地址是 http://，
             Android 9+ 默认禁止明文流量，否则扫码后直接白屏/加载失败。 -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WebViewActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|density|fontScale"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/Theme.DshBridge.Web"
            android:windowSoftInputMode="adjustResize" />

        <!-- 扫码页：fullSensor 让取景界面跟随手机方向（库自带的 CaptureActivity 锁横屏，体验别扭） -->
        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.DshBridge.Scanner" />

        <!-- 供系统安装器读取下载好的 APK：Android 7+ 不允许以 file:// 形式外传文件 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 移除 ZXing 库自带的 CaptureActivity：它是固定横屏、界面不可定制的旧扫码页，
             本项目改用自定义 ScanActivity（DecoratedBarcodeView + 相册入口）。
             不移除的话它会一直留在合并清单里，容易被误用。 -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            tools:node="remove" />
    </application>

</manifest>
